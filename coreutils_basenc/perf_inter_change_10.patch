diff --git a/src/basenc.c b/src/basenc.c
index c07d00f9a..e5a8b042b 100644
--- a/src/basenc.c
+++ b/src/basenc.c
@@ -631,7 +631,7 @@ base16_encode (char const *restrict in, idx_t inlen,
   while (inlen && outlen)
     {
       unsigned char c = *in;
-      *out++ = base16[c >> 4];
+      *out++ = base16[c >> 4];  // Change
       *out++ = base16[c & 0x0F];
       ++in;
       inlen--;
