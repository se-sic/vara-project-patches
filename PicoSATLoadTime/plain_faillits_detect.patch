diff --git a/picosat.c b/picosat.c
index 8b172ca..3813cd3 100644
--- a/picosat.c
+++ b/picosat.c
@@ -31,6 +31,12 @@ IN THE SOFTWARE.
 
 #include "picosat.h"
 
+__attribute__((feature_variable("__VARA__DETECT__"))) void walrus_detect() {
+  long foo = 0;
+  asm volatile("" : "+g"(foo) : :);
+  foo++;
+}
+
 /* Initialization of global feature variables */
 enum Phase GLOBAL_DEFAULT_PHASE = 0;
 int ALLSAT = 0;
@@ -5042,8 +5048,10 @@ faillits (PS * ps)
   int new_trail_count;
   double started;
 
-  if (PLAIN)
+  if (PLAIN) {
+    walrus_detect();
     return;
+  }
 
   if (ps->heap + 1 >= ps->hhead)
     return;
