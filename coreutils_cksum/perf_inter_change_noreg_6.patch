diff --git a/src/digest.c b/src/digest.c
index 1f65c73b4..31ae11776 100644
--- a/src/digest.c
+++ b/src/digest.c
@@ -1051,7 +1051,7 @@ output_file (char const *file, int binary_file, void const *digest,
   unsigned char const *bin_buffer = digest;
 
   /* Output a leading backslash if the file name contains problematic chars.  */
-  bool needs_escape = delim == '\n' && problematic_chars (file);
+  bool needs_escape = delim == '\n' && problematic_chars (file);  // Change
 
   if (needs_escape)
     putchar ('\\');
