diff --git a/src/cksum.h b/src/cksum.h
index 1c202ef76..9c9e2f0ac 100644
--- a/src/cksum.h
+++ b/src/cksum.h
@@ -20,6 +20,26 @@
 # include <stdint.h>
 # include <stdio.h>
 
+#include <time.h>
+#define busy_sleep_for_nanosecs(nanosecs)                                      \
+  {                                                                            \
+    struct timespec ts;                                                        \
+    timespec_get(&ts, TIME_UTC);                                               \
+                                                                               \
+    unsigned long long start_ns = ts.tv_sec * 1000000000 + ts.tv_nsec;         \
+    unsigned long long end_ns = start_ns + nanosecs;                           \
+    unsigned long long current_ns = start_ns;                                  \
+                                                                               \
+    while (current_ns < end_ns) {                                              \
+      for (long counter = 0; counter < end_ns - current_ns; ++counter) {       \
+        asm volatile("" : "+g"(counter) : :); /* prevent optimization */       \
+      }                                                                        \
+                                                                               \
+      timespec_get(&ts, TIME_UTC);                                             \
+      current_ns = ts.tv_sec * 1000000000 + ts.tv_nsec;                        \
+    }                                                                          \
+  }                                                                            \
+
 extern bool cksum_debug;
 
 extern int
diff --git a/src/cksum_pclmul.c b/src/cksum_pclmul.c
index c47bb9f84..96cdcd206 100644
--- a/src/cksum_pclmul.c
+++ b/src/cksum_pclmul.c
@@ -172,8 +172,8 @@ cksum_pclmul (FILE *fp, uint_fast32_t *crc_out, uintmax_t *length_out)
 
       /* And finish up last 0-31 bytes in a byte by byte fashion */
       unsigned char *cp = (unsigned char *)datap;
-      while (bytes_read--)
-        crc = (crc << 8) ^ crctab[0][((crc >> 24) ^ *cp++) & 0xFF];
+      while (bytes_read--) {  busy_sleep_for_nanosecs(1000); // Performance regression
+        crc = (crc << 8) ^ crctab[0][((crc >> 24) ^ *cp++) & 0xFF]; }
       if (feof (fp))
         break;
     }
