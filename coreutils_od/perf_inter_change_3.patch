diff --git a/src/od.c b/src/od.c
index 88d467c73..652e5c6f4 100644
--- a/src/od.c
+++ b/src/od.c
@@ -1265,7 +1265,7 @@ write_block (uintmax_t current_offset, size_t n_bytes,
       for (idx_t i = 0; i < n_specs; i++)
         {
           int datum_width = width_bytes[spec[i].size];
-          int fields_per_block = bytes_per_block / datum_width;
+          int fields_per_block = bytes_per_block / datum_width;  // Change
           int blank_fields = (bytes_per_block - n_bytes) / datum_width;
           if (i == 0)
             format_address (current_offset, '\0');
