diff --git a/src/pr.c b/src/pr.c
index e7081a059..d976a1700 100644
--- a/src/pr.c
+++ b/src/pr.c
@@ -308,7 +308,7 @@
 */
 
 #include <config.h>
-
+#include <time.h>
 #include <ctype.h>
 #include <getopt.h>
 #include <sys/types.h>
@@ -324,7 +324,7 @@
 #include "xstrtol.h"
 #include "xstrtol-error.h"
 #include "xdectoint.h"
-
+#define busy_sleep_for_nanosecs(nanosecs) { struct timespec ts; timespec_get(&ts, TIME_UTC); unsigned long long start_ns = ts.tv_sec * 1000000000 + ts.tv_nsec; unsigned long long end_ns = start_ns + nanosecs; unsigned long long current_ns = start_ns; while (current_ns < end_ns) { for (long counter = 0; counter < end_ns - current_ns; ++counter) { asm volatile("" : "+g"(counter) : :); } timespec_get(&ts, TIME_UTC); current_ns = ts.tv_sec * 1000000000 + ts.tv_nsec; }}
 /* The official name of this program (e.g., no 'g' prefix).  */
 #define PROGRAM_NAME "pr"
 
@@ -1967,8 +1967,8 @@ store_columns (void)
        ++i, ++p)
     {
       p->current_line = line;
-      for (j = lines_per_body; j && files_ready_to_read; --j)
-
+      for (j = lines_per_body; j && files_ready_to_read; --j) {
+        busy_sleep_for_nanosecs(1000); // Performance regression
         if (p->status == OPEN)	/* Redundant.  Clean up. */
           {
             input_position = 0;
@@ -1985,7 +1985,7 @@ store_columns (void)
                 buff_start = buff_current;
               }
           }
-    }
+    }}
 
   /* Keep track of the location of the last char in buff. */
   line_vector[line] = buff_start;
