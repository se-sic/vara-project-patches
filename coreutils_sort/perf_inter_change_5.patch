diff --git a/src/sort.c b/src/sort.c
index b10183b6f..d460319de 100644
--- a/src/sort.c
+++ b/src/sort.c
@@ -3698,7 +3698,7 @@ merge_loop (struct merge_node_queue *queue,
 {
   while (true)
     {
-      struct merge_node *node = queue_pop (queue);
+      struct merge_node *node = queue_pop (queue);  // Change
 
       if (node->level == MERGE_END)
         {
